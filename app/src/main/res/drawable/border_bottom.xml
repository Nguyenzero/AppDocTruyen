<!-- res/drawable/border_bottom.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Nền trắng -->
    <size android:height="1dp"/> <!-- Chiều cao 1dp -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray"/> <!-- Viền dưới màu xám -->
</shape>
